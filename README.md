# Румикуб

Многопользовательский Румикуб: играйте с друзьями по коду комнаты (до 4 человек) или против компьютерных соперников. React-клиент + Node.js-сервер (Socket.io), всё в одном процессе.

## Возможности

- Создание комнаты и подключение друзей по 5-символьному коду.
- Добавление ботов (лёгкий / средний / сложный).
- Честные правила: первая выкладка от 30 очков, манипуляции столом, джокеры, подсчёт очков.
- Тёмное удобное UI, живые подсказки о корректности групп.

## Разработка

```bash
npm install   # ставит зависимости корня и клиента
npm run dev   # сервер :3001 + Vite :5173 с hot-reload
```

## Запуск на сервере (для друзей)

```bash
npm ci && npm run build
NODE_ENV=production PORT=3001 node server/src/index.js
```

Либо через Docker:

```bash
docker build -t rummikub .
docker run -d -p 3001:3001 -e PORT=3001 -v rummikub-data:/app/server/data rummikub
```

**Важно:** том `rummikub-data` хранит таблицу лидеров и профили игроков
(`/app/server/data/players.json`). Без него очки сбрасываются при пересоздании контейнера.
Если том не нужен (перезапуск с сохранением очков не планируется), флаг `-v` можно опустить.

После запуска откройте в браузере `http://<адрес сервера>:3001`. Друзья подключаются по этому же адресу. Снаружи нужен только порт 3001 (http + WebSocket в одном месте). Для публичного доступа рекомендуется reverse-proxy с HTTPS (nginx/caddy) поверх 3001.

Переменные окружения:

- `PORT` — порт сервера (по умолчанию `3001`).
- `NODE_ENV=production` — включать раздачу собранного клиента из `client/dist`.
- `PLAYERS_FILE` — путь к файлу таблицы лидеров (по умолчанию `server/data/players.json`, в Docker — `/app/server/data/players.json`).

## Проверки

```bash
npm run check   # собрать клиент + прогнать тесты правил
node server/test/run-tests.mjs
```

Полное описание архитектуры, правил и сетевого протокола — в [AGENTS.md](AGENTS.md).
